import { SearchPlaceholderPipe } from './search-placeholder.pipe';

describe('SearchPlaceholderPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPlaceholderPipe();
    expect(pipe).toBeTruthy();
  });
});
